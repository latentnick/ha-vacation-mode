[project]
name = "lights"
version = "0.1.0"
description = "Vacation light scheduler via empirical-day resampling of Home Assistant history"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "influxdb>=5.3.2",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
]
